import styled from "styled-components";
import Input from "./Input";

const FormComponent: any = styled.div`
  padding: 2rem;
  margin-top: 2rem;
`;

FormComponent.Input = Input;

export default FormComponent;
